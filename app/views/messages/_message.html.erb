<li class="<%=  self_or_other(message) %>">
  <div class="avatar">
  	<% if self_or_other(message) == 'self' %>
      <img class="img-circle" src="http://www.gravatar.com/avatar/<%= @current_user_sha %>.jpg?s=25" />
  	<% else %>
  	  <img class="img-circle" src="http://www.gravatar.com/avatar/<%= @recipient_sha %>.jpg?s=25" />
  	<% end %>
  </div>
  <div class="chatboxmessagecontent">
    <p><%= message.body %></p>
    <time datetime="<%= message.created_at %>" title="<%= message.created_at.strftime("%d %b  %Y at %I:%M%p") %>">
      <%= message_interlocutor(message).name %> • <%= message.created_at.strftime("%H:%M %p") %>
    </time>
  </div>
</li>
